<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled-Zustand (deaktiviert) -->
    <!-- muss an erster Stelle der item-List sein, geht sonst nicht -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <gradient android:startColor="#E6E7EF"
                android:endColor="#959595"
                android:angle="270" />
            <corners android:radius="10dp" />
            <stroke android:width="3px" android:color="@color/black" />
            <size
                android:width="100dp"
                android:height="50dp"
                />
        </shape>
    </item>
    <!-- Normaler Zustand (kein Klick) -->
    <item android:state_pressed="false">
        <shape android:shape="rectangle">
            <solid android:color="#5263BD" />
            <corners android:radius="10dp" />
            <stroke android:width="3px" android:color="@color/black" />
            <size
                android:width="100dp"
                android:height="50dp"
                />
        </shape>
    </item>
    <!-- Gedrückter Zustand (beim Klicken) -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#5263BD" />
            <gradient android:startColor="#E6E7EF"
                android:endColor="#5263BD"
                android:angle="270" />
            <corners android:radius="10dp" />
            <stroke android:width="3px" android:color="@color/black" />
            <size
                android:width="100dp"
                android:height="50dp"
                />
        </shape>
    </item>
</selector>